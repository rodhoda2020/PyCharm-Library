from flask_sqlalchemy import SQLAlchemy

db = SQLAlchemy()
# We have a object called SQLAlchemy that will link to the app
# and look at all the objects we tell it to
# and allows us to map those objects as rows and columns
# into the database
